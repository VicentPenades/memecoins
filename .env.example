NODE_ENV=development

# Neon PostgreSQL connection string.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Random secret of at least 32 characters used to hash visitor IP addresses.
VOTE_IP_HASH_SECRET=
